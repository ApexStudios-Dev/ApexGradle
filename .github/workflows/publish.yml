on:
    workflow_dispatch:
    push:
        branches: [ "**/main" ]
        paths: [ "**/src/**" ]

jobs:
    publish:
        uses: ApexStudios-Dev/.github/.github/workflows/publish-snapshot.yml@master
        secrets: inherit
        with:
            github_package_url: "https://maven.pkg.github.com/ApexStudios-Dev/ApexGradle"
